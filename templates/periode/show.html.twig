{% extends 'base.html.twig' %}

{% block title %}Periode
{% endblock %}

{% block body %}
	{{include("sidebar.html.twig")}}

	<style>
		body {
			background-color: #eee;
		}
	</style>

	<div class="home_content" style="margin-top:200px; height: 100%;">

		<h1>Periode</h1>

		<table class="table table-bordered table-white" style="background-color:#fff;width:900px; border:solid 1px; margin-right:auto; margin-left:auto;">
			<thead>


				<div style="width:900px;height:80px; background-color:#eee;margin-right:auto; margin-left:auto">
					<center>

						<a href="{{ path('periode_index') }}">
							<button class="btn" style="border:solid 1px;width:900px;height:80px;margin-right:auto; margin-left:auto">
								<h1>back to list</h1>
							</button>
						</a>

					</center>
				</div>

				<div style="width:900px;height:60px; margin-right:auto; margin-left:auto">


					<a href="{{ path('periode_edit', {'id': periode.id}) }}">
						<button class="btn btn-outline-success" style="width:900px;height:60px;margin-right:auto; margin-left:auto">
							<h1>Edit</h1>
						</button>
					</a>


				</div>

				<tr>
					<th scope="col">Information</th>
					<th scope="col" style="width:200px;">Details</th>

				</tr>
			</thead>
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ periode.id }}</td>
				</tr>
				<tr>
					<th>Trimestre</th>
					<td>{{ periode.trimestre }}</td>
				</tr>
			</tbody>
		</table>

		{# <a href="{{ path('periode_index') }}">back to list</a>
		
				<a href="{{ path('periode_edit', {'id': periode.id}) }}">edit</a> #}

		{{ include('periode/_delete_form.html.twig') }}
	{% endblock %}

{{ form_start(form) }}
{{ form_widget(form) }}
<div style="margin-right:auto;margin-left:auto; width:90px;height:70px;">

	<button class="btn btn-outline-info" style="border: solid 1px; width:90px">{{ button_label|default('Save') }}</button>
</div>

{{ form_end(form) }}

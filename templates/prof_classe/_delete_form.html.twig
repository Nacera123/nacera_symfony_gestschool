<form method="post" action="{{ path('prof_classe_delete', {'id': prof_classe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prof_classe.id) }}">
    <button class="btn">Delete</button>
</form>
